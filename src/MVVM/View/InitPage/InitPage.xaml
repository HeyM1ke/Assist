<Window x:Name="InitWindow" x:Class="Assist.MVVM.View.InitPage.InitPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:lang="clr-namespace:Assist.Properties.Languages"
        xmlns:local="clr-namespace:Assist.MVVM.View.InitPage"
        mc:Ignorable="d"
        Title="ValorAssist" Height="350" Width="550" WindowStyle="None" WindowStartupLocation="Manual" AllowsTransparency="True" Loaded="InitWindow_Loaded" Background="Transparent">
    <Window.Resources>
        <Style x:Key="statusText" TargetType="{x:Type Label}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="WhiteSmoke"/>
            <Setter Property="Content" Value="{x:Static lang:Lang.InitPage_Loading}"/>
        </Style>
    </Window.Resources>
    
    
    <Grid Background="{StaticResource AssistBackground}">
        <Image Source="/Resources/Assist_Logo_White.png" VerticalAlignment="Center" HorizontalAlignment="Center" Height="200" RenderOptions.BitmapScalingMode="HighQuality">
            <Image.Effect>
                <DropShadowEffect BlurRadius="50" ShadowDepth="7"></DropShadowEffect>
            </Image.Effect>
        </Image>
        <Label Content="{Binding Path=CurrentStatusMessage}" HorizontalAlignment="Center" VerticalAlignment="Center" FontFamily="Bahnschrift Light" FontSize="24" Foreground="White" Opacity=".8" Margin="0,200,0,0" Width="auto">
            <Label.Effect>
                <DropShadowEffect BlurRadius="1" ShadowDepth="1"/>
            </Label.Effect>
        </Label>
    </Grid>
</Window>
