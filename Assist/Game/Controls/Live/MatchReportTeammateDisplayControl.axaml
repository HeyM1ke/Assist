<Styles xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="using:Assist.Game.Controls.Live"
        xmlns:asyncImageLoader="clr-namespace:AsyncImageLoader;assembly=AsyncImageLoader.Avalonia">
    <Design.PreviewWith>
        <controls:MatchReportTeammateDisplayControl Width="264"
                                                    Height="20"
                                                    AgentIcon=""
                                                    RankIcon=""
                                                    Statline=""
                                                    />
    </Design.PreviewWith>

    <Style Selector="controls|MatchReportTeammateDisplayControl">
        <!-- Set Defaults -->
        <Setter Property="Template">
            <ControlTemplate>
                <Grid ColumnDefinitions="152, *, Auto">
                    <StackPanel Orientation="Horizontal"
                                Spacing="3">
                        <asyncImageLoader:AdvancedImage Width="20"
                                                        Height="20"
                                                        Source="{TemplateBinding AgentIcon}"/>
                        <TextBlock Text="{TemplateBinding TeammateName}"
                                   FontFamily="{StaticResource Inter}"
                                   FontWeight="Regular"
                                   FontSize="11"
                                   TextAlignment="Left"
                                   MaxWidth="150"
                                   VerticalAlignment="Center"
                                   Foreground="{DynamicResource AssistTextWhite}"/>
                    </StackPanel>
                    
                    <asyncImageLoader:AdvancedImage Source="{TemplateBinding RankIcon}"
                                                    Margin="3"
                                                    Grid.Column="1"/>
                    
                    <TextBlock Text="{TemplateBinding Statline}"
                               FontFamily="{StaticResource Inter}"
                               FontWeight="Regular"
                               FontSize="11"
                               MinWidth="70"
                               TextAlignment="Right"
                               VerticalAlignment="Center"
                               Grid.Column="2"
                               Foreground="{DynamicResource AssistTextWhite}"/>
                </Grid>
            </ControlTemplate>
        </Setter>
    </Style>
</Styles>
